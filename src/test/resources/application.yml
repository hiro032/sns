spring:
  jpa:
    hibernate:
    format_sql: true
      ddl-auto: update
    show-sql: true
    database: h2

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test;
    username: sa
    password:

jwt:
  secret-key: hiro.sns_2022_secret_key
